package it.italiandudes.despicable_cards.data.enums;

public enum ServerRejectReason {
    UNKNOWN_ERROR,
    INVALID_PASSWORD,
    LOBBY_FULL,
    USERNAME_TAKEN,
    NOT_IN_LOBBY,
    INVALID_USERNAME
}
